language: node_js
env: CI=true
node_js:
  - stable
services:
  - mongodb
before_script:
  - npm install -g gulp
  - npm install -g sally-js
  - mkdir logs
  - mongo <drop-db
  - ./bin/upgrade-model
script:
  - gulp
  - sally verify logs/*.sal
  - pkg=$(npm pack)
  - npm install -g --quiet $pkg
